spring:
  application:
    name: empservice
  datasource:
    url: jdbc:mysql://localhost:3306/empmanager
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver

#spring:
#  jpa:
#    hibernate:
#      ddl-auto: update

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
    register-with-eureka: true
    fetch-registry: true
server:
  port: 9000
